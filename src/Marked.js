import React from 'react';
import marked from 'marked';


console.log(marked('add'))
class Marked extends React.Component {
  render () {

    return(
      <div>
        
      </div>
    )
  }
}

export default Marked;
